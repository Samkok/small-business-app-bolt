name: Deploy iOS to TestFlight

on:
  workflow_dispatch:  # manual trigger only

jobs:
  build-and-submit-ios:
    name: Build and Submit iOS
    runs-on: ubuntu-latest

    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}  # ✅ Authenticates EAS in all steps

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Install EAS CLI
        run: npm install -g eas-cli

      - name: 📂 Install dependencies
        run: npm install

      - name: 🛠️ Build and Submit iOS app for TestFlight
        run: eas build -p ios --submit --non-interactive
        env:
          EAS_APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.EAS_APPLE_APP_SPECIFIC_PASSWORD }}
